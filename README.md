# Creds-Manager
Lightweight CRUD app for managing credentials locally using PostgREST and PostgreSQL. Features a clean dark UI with add, remove, and live-list functionality — served via Python HTTP server with no Node.js or frameworks required.
